# 视觉设计规范 (Visual System)

> **目标**: 确立全局视觉基调，规范组件尺寸与状态，输出可开发的切图资源。

## 1. 风格关键词

- **Galgame**: 精致的人物立绘，强调情感氛围。
- **Skeuomorphism (拟物)**: 信封、火漆印、邮票齿孔、纸张纹理。
- **Glassmorphism (玻璃拟态)**: 模态框背景采用高斯模糊 (Backdrop Blur)，增加现代感。

## 2. 全局设计变量 (Design Tokens)

### 2.1 色彩体系 (Color Palette)

- **Primary (主色)**: `#0ea5e9` (海大蓝/Ocean Blue) - 用于强调、按钮、Link。
- **Background (背景)**: `#fdfbf7` (米白/信纸色) - 用于全局背景，营造温暖怀旧感。
- **Text (正文)**: `#292524` (Stone-800) - 偏暖深灰，阅读舒适。
- **Muted (辅助)**: `#78716c` (Stone-500) - 用于次要信息。
- **Border (边框)**: 待定

### 2.2 字体排印 (Typography)

- **Headings (标题)**: `Noto Serif SC` (宋体) - 营造文学感与回忆感。
- **Body (正文)**: `Noto Sans SC` (黑体) - 确保多终端可读性。
- **Handwriting (手写)**: 用于“背后的故事”或签名 (待定: WebFont 或 SVG)。

### 2.3 玻璃拟态与阴影

- **模态框背景**:
  - 透明度: `待定`
  - 模糊度 (Blur): `10px-20px`
  - 边框: 1px 白色半透明描边 (可选)
- **卡片阴影**: 需定义常规态与悬浮态 (Hover)。

## 3. 关键视觉资产清单 (Assets Checklist)

### 3.1 开场信封 (Envelope Intro)

为了实现“开信”动画及新增的滚动页面，需要：
- [ ] **信封组件**: 背面、盖子、信纸、火漆印 (同上)。
- [ ] **关于页面**: 
  - 页面背景纹理 (纸张/水彩)。
  - **大标题设计**: “与她的海大时光笺” (SVG/高清 PNG)。
  - 团队成员头像/展示图。

### 3.2 海大地图 (The Map)

- **PC 端**:
  - 格式: SVG (最佳) 或 高清 PNG (宽 >= 2560px)。
  - 风格: 扁平化/手绘风/等轴测。
  - 注意: 四周预留背景色，防止裁切。
- **移动端**:
  - 格式: 竖版/方形静态图 (仅供查看)。

### 3.3 邮票与标记 (Stamps & Pins)

- **地图坐标点 (Pin)**:
  - Q版头像边框容器 (48x48 或 64x64)。
- **邮票样式 (Stamp Frame)**:
  - 齿孔边框素材 (中间镂空)。
  - 宽高比固定 (3:4 或 1:1)。

### 4.1 PC 端时光卷轴 (Scroll Strip)
- **卷轴主体**:
  - **背景**: 具有纸张感的米黄色渐变 (`linear-gradient(to right, #ede6d9 50%, rgba(237,230,217,0.6))`)。
  - **宽度**: 固定 `56px`。
- **阴影与厚度**:
  - **外部投影**: `5px 0 25px rgba(139,90,43,0.22)`，强化浮出感。
  - **内部阴影**: `inset -2px 0 0 rgba(139,90,43,0.15)`，模拟卷纸边缘的立体感。
- **装饰元素**:
  - **卷纸横纹**: 间隔分布的淡褐色线条 (`rgba(120,70,20,0.15)`)，模拟纸张纤维感。
  - **交互指引**: “展开地图”竖排文字与向左箭头，对比度需保持在 `0.6` 以上。

### 4.2 PC 端故事卡片堆 (Story Card Stack)
- **层级**: `z-index: 30` (高于卷轴层)，支持卡片覆盖卷轴。
- **卡片**: 纯图片，无边框，**自适应比例** (Adaptive Ratio)。
- **文本区**: 
  - 居中布局，默认隐藏。
  - 包含**静态邮票** (Stamp) 装饰。
  - 背景可带轻微纸张纹理。

### 4.2 移动端卡片流
- **封面图**: 统一比例 (4:3 或 16:9)。
- **详情页**: 上图下文。
- **FAB**: 右下角悬浮地图按钮。

## 5. 交付格式要求

1. **设计稿**: Figma (Dev Mode) 或 蓝湖。
2. **切图**:
   - 图标/Logo/线条 -> **SVG**。
   - 照片/插画 -> **WebP** / **PNG** (@2x/@3x)。
3. **命名**: `component_state.format` (e.g., `stamp_frame_active.png`).
